# Native visual baseline manifest

## v2.2 deterministic snapshot contract

The repository now has an executable, test-only visual regression layer. Its canonical profile is **iPhone 17e, iOS 26.5, portrait, 393 × 852 points at 3×, Light appearance, `en_US`, and Large content size**. The Trip Overview accessibility baseline uses AX XXXL. Tests render production views/controllers with fixed local fixture state; they never use production accounts, network responses, MapKit tiles, remote media, the current clock, or random values.

`AlmidySnapshotTesting` in `AppTests/VisualRegression` owns deterministic rendering, baseline lookup, strict RGBA comparison, and failure attachments. It allows **zero differing pixels and zero channel delta**. A failure attaches the expected image, actual image, and a magenta difference image. Missing baselines fail by default, and normal test runs can never overwrite approved images.

Recording is an explicit local maintainer action. A shell environment variable does not reliably propagate into a simulator test process, so set and clear the simulator launch environment around the focused recording run:

```sh
xcrun simctl boot "iPhone 17e"
xcrun simctl bootstatus "iPhone 17e" -b
xcrun simctl spawn booted launchctl setenv ALMIDY_RECORD_SNAPSHOTS 1
xcodebuild test -project ios/App/App.xcodeproj -scheme App -destination 'platform=iOS Simulator,name=iPhone 17e,OS=26.5' -testLanguage en -testRegion US -only-testing:AppTests/NativeTripOverviewSnapshotTests -only-testing:AppTests/NativeVisualRegressionTests
xcrun simctl spawn booted launchctl unsetenv ALMIDY_RECORD_SNAPSHOTS
```

Always clear the variable, inspect every changed PNG, and commit baseline changes separately from unrelated implementation. The ordinary non-recording command is the same command without the two `launchctl` lines and without `ALMIDY_RECORD_SNAPSHOTS`. `AlmidySnapshotHarnessTests` independently verifies the profile, mismatch behavior, naming, PNG decoding, and expected baseline count.

### Approved pixel baselines

| Baseline | Deterministic fixture | Profile | Protection |
|---|---|---|---|
| `TripOverview/trip-overview-populated-expanded.png` | Fixed Barcelona trip, local hero, itinerary/documents/expenses | Large | Pixel + existing geometry |
| `TripOverview/trip-overview-populated-collapsed.png` | Same trip at the current scrolled/collapsed presentation | Large | Pixel + existing geometry |
| `TripOverview/trip-overview-empty.png` | Fixed empty Barcelona trip and local hero | Large | Pixel + existing geometry |
| `TripOverview/trip-overview-accessibility.png` | Fixed populated trip and local hero | AX XXXL | Pixel + existing geometry |
| `Globe/globe-active-trip-card.png` | Fixed active trip and generated local media | Large | Pixel + existing geometry |
| `Globe/globe-future-trip-card.png` | Fixed future trip and generated local media | Large | Pixel + existing geometry |
| `Globe/globe-reservation-automation.png` | Fixed importer suggestion copy/actions | Large | Pixel + existing geometry |
| `Globe/globe-map-controls.png` | Controls over a fixed map-like color surface | Large | Pixel + existing geometry |
| `PlaceCard/place-card-travel-modes.png` | Fixed Walk/Drive/Transit/Cycle durations and selection | Large | Pixel + existing geometry |
| `Editors/saved-place-populated.png` | Fixed populated saved-place component | Large | Pixel + existing geometry |
| `Editors/transportation-train.png` | Fixed Train component with departure/arrival data | Large | Pixel + existing geometry |
| `Editors/flight-populated.png` | Fixed populated Flight component | Large | Pixel + existing geometry |

### Tier 1 coverage disposition

| Surface/state family | Pixel baseline | Deterministic geometry/behavior | Disposition |
|---|---:|---:|---|
| Trip Overview populated, collapsed, empty, accessibility | Yes | Yes | Both |
| Globe active/future trip cards, importer, map controls | Yes | Yes | Both |
| Place Card travel-mode selector | Yes | Yes | Both |
| Saved Place, Train transportation, Flight feature components | Yes | Yes | Both |
| Full Globe/map sheet and annotations | No | Yes | Geometry-only: MapKit tiles, placement, and runtime trip state are not stable pixels |
| Map Preferences | No | Yes | Geometry-only: `MKMapSnapshotter` preview imagery is Apple-service data |
| Full Place Card | No | Yes | Geometry-only: Look Around, routes, ETA, and `MKMapItem` state are service-owned |
| Native Itinerary and date picker | No | Yes | Geometry-only: full controller still requires authenticated trip/data state |
| Search result states | No | Yes | Geometry-only: `MKLocalSearchCompletion` has no deterministic public constructor |
| Settings/auth/account | No | Yes | Geometry-only: account variants require authenticated state |
| Cost, note, date, and time system-input states | No | Yes | Geometry-only: keyboard, calendar, locale, and wheel rasterization/timing are system-owned |

This infrastructure is CI-ready but should only become a required hosted pixel gate when CI pins the same Xcode build, iOS 26.5 runtime, iPhone 17e device profile, locale, scale, and font rasterization. CI must retain the three failure attachments for review. Until those prerequisites are pinned, run the pixel suite on the canonical local simulator and keep the deterministic behavior/geometry suite authoritative in CI.

Phase 0 baseline target: iPhone 16 Pro simulator, 393 × 852 points, light appearance, standard accessibility contrast, Large Dynamic Type unless a row states otherwise.

| State | Data/conditions | Dynamic Type | Capture status |
|---|---|---|---|
| Globe | authenticated native map, populated trip list | Large | Pending reproducible authenticated fixture |
| Trip Overview populated | trip with itinerary, documents, expenses | Large | Pending fixture/capture harness |
| Trip Overview empty | trip with no itinerary records | Large | Pending fixture/capture harness |
| Trip Overview collapsed | populated fixture at collapsed detent | Large | Pending fixture/capture harness |
| Trip Overview accessibility | populated fixture | AX XXXL | Pending fixture/capture harness |
| Native Itinerary | multi-day populated fixture; verify 50×50 timeline/more controls | Large | Phase 2 geometry contract covered by unit tests; authenticated image capture still pending fixture harness |
| Native Itinerary date picker | `NativeItineraryDatePickerViewController`; page sheet, custom 67% detent, 30pt radius, visible grabber, 67pt header, 82×44 Cancel control, 1pt divider, 18pt calendar side insets | Large | Phase 3 geometry contract covered by deterministic tests; authenticated launch fixture still pending |
| New Activity | default category list | Large | Pending fixture/capture harness |
| Place Details | stable Apple MapKit result | Large | Pending network-stable fixture |
| Saved-place editor | prefilled Stay | Large | Pending fixture/capture harness |
| Transportation editor | prefilled flight/route | Large | Pending fixture/capture harness |
| Create Trip | destination step | Large | Pending fixture/capture harness |
| Settings | default preferences | Large | Pending fixture/capture harness |
| Settings signed out | `NativeSettingsViewController`; utility page sheet, large detent, 28pt radius, visible grabber, edge-attached compact width, 52pt close control at 14pt top/20pt trailing, 38pt leading title, 24pt cards with 1pt subtle border, 64pt native rows | Large | Phase 4 deterministic geometry contracts added; controller still depends on presenter callbacks, so screenshot fixture remains pending |
| Settings signed in | Same presentation geometry; profile card minimum height 238pt and existing profile popover behavior | Large | Authentication-free screenshot fixture not created; no user data captured |
| Account | authenticated account | Large | Pending authenticated fixture |
| Auth | signed-out state | Large | Pending fixture/capture harness |
| Search initial | `NativeMapSearchViewController`; utility page sheet, medium/large detents, medium initially selected by UIKit, 28pt radius, visible grabber; Cancel top 10/minimum 44; centered 30pt title, 17pt subtitle; 54pt outlined search field at 24pt horizontal inset; inline initial message | Large | Phase 5 deterministic shared-contract coverage; extracted controller has no auth dependency, but no production screenshot was captured because result states depend on live MapKit completion data |
| Search results | Query of 2+ characters; up to four native MapKit completion rows, 68pt row height, 272pt bordered results surface, 16pt title/13pt subtitle | Large | Fixture not added: `MKLocalSearchCompletion` does not provide a clean deterministic public initializer; live screenshot would not be reproducible |
| Search no results/error | Existing message-only state below the field; exact copy retained for initial, searching, no-results, completion failure, resolving, and resolution failure states | Large | Message-only empty-state contract covered by unit tests; no fabricated network state capture |
| Total Cost empty/focused | `NativeTotalCostInputViewController`; editor page sheet with large selected detent, 34pt radius, hidden grabber, edge-attached compact width; 62pt centered header, 76×34 Cancel and 60×34 Save at 16pt edges/14pt top; `$` + `0.00` 52pt baseline row centered 160pt above view center; 125pt amount width, 34pt minimum font, decimal pad; 40pt currency menu 16pt above keyboard | Fixed measured typography | Phase 6A deterministic controller fixture and geometry tests added on iPhone 17 Pro Max simulator; no screenshot persisted because test-host sheet and software-keyboard capture timing was not reproducible |
| Total Cost populated/invalid | Same geometry; currency menu updates symbol/code; valid save emits Decimal/code/display string after dismissal; invalid text shakes the field and announces the existing validation copy | Fixed measured typography | Business behavior retained unchanged; production initializer has no prefilled-value contract, so no artificial populated production state or screenshot hook was added |
| Note empty/focused | `NativeNoteInputViewController`; editor page sheet with large selected detent, 34pt radius, hidden grabber, edge-attached compact width; plain system background; 62pt centered header, 76×34 secondary Cancel and 60×34 accent Save at 16pt edges/14pt top; clear native `UITextView` from y=66 to 8pt above keyboard with 16pt outer margins | Fixed 18pt text | Phase 6B deterministic controller fixture and geometry tests added on iPhone 17 Pro Max simulator; no placeholder exists; keyboard-visible screenshot not persisted because test-host attachment timing is not reproducible |
| Note multiline/cleared | Native multiline selection, scrolling, Return/newlines, default keyboard configuration, 8pt vertical text-container inset, 5pt line-fragment padding; Save always enabled and trims surrounding whitespace/newlines while preserving internal newlines; cleared content emits an empty string | Fixed 18pt text | Empty, multiline, and cleared-save states covered by deterministic tests; no production initial-value API exists, so no artificial populated initializer or screenshot hook was added |
| Flight Date default/selected | `NativeFlightDateInputViewController`; editor page sheet, custom `min(700, maximum × 0.79)` detent, 34pt radius, hidden grabber; 82×40 Cancel and 68×40 Save at 20pt edges; native inline date picker at 18pt sides and 405pt height; two 58pt Today/Tomorrow shortcuts; 48pt Clear Date & Time action | Existing fixed UIKit typography | Phase 6C deterministic controller fixture and geometry tests added on iPhone 17 Pro Max simulator; native calendar capture not persisted because locale/calendar rendering would not be a stable repository baseline |
| Flight Date shortcut/cleared | Today and Tomorrow normalize with `Calendar.current.startOfDay`; shortcuts save immediately after dismissal; picker Save emits its selected value; Clear emits nil and preserves the parent transportation editor's existing relationship reset behavior | Existing fixed UIKit typography | Start state, picker save, clear payload, and measured geometry covered by focused tests; parent date/time relationship logic remains unchanged |
| Flight Time default/selected | `NativeFlightTimeInputViewController`; editor page sheet, custom `min(410, maximum × 0.47)` detent, 34pt radius, hidden grabber; 78×36 Cancel and 64×36 Save at 20pt edges; native 190pt wheel time picker with one-minute interval; full-width 42pt time-zone and Clear Time actions at 14pt sides | Existing fixed UIKit typography | Phase 6C deterministic controller fixture and geometry tests added on iPhone 17 Pro Max simulator; wheel/date formatting capture not persisted because locale, 12/24-hour preference, and stored time-zone state would make it non-reproducible |
| Flight Time changed/cleared | Wheel changes preserve the editor's selected calendar day, replace hour/minute, and zero seconds using `Calendar.current`; formatter and picker use `NativeTimeZonePreference`; searchable time-zone selection retains stored-preference/notification behavior; Save emits the recombined date and Clear emits nil | Existing fixed UIKit typography | Wheel contract, time-zone binding, recombination semantics, clear payload, and measured geometry covered by focused tests; parent automatic-arrival and departure/arrival relationships remain unchanged |
| Reservation/optional details | `NativeReservationDetailsView` embedded in non-Location manual activity editors; unbordered system-background surface, 18pt radius, 6pt vertical/16pt horizontal margins; ordered native editable rows at 52pt, 138pt label column, 18pt field spacing, 0.5pt separators; route-kind labels/placeholders and model mapping remain owner-supplied | Fixed 17pt UIKit typography | Phase 6D empty/populated-edit geometry and callback contracts covered by deterministic tests. Location retains its original local implementation and is deferred to Phase 6E; no screenshot fixture exists for the MapKit-dependent parent editor |
| Save Link alert | `NativeLinkInputAlert`; system alert titled Save Link with existing flight-specific message, `https://` placeholder, URL keyboard, no autocapitalization/correction; trims outer whitespace, accepts only HTTP/HTTPS, derives display name by removing `www.`, and silently retains the alert's existing no-op invalid Save behavior | System alert typography | Input configuration and deterministic valid/invalid URL rules covered by tests; system alert screenshot not persisted because it adds no stable Almidy-owned visual surface |
| Attachment launcher | `NativeAttachmentActionView`; 64pt system-background row, 18pt radius, 15×18pt margins, 28pt decorative folder icon, 18pt icon/title gap, 19pt semibold title; full-row primary menu titled Add Document with Import Document, Save Link, Choose Photo from Library, and Take a Photo | Fixed measured typography | Menu/action-launch contract and geometry covered by deterministic tests. Document, photo-library, and camera picker UI remains system-owned and is intentionally excluded from visual baselines |
| Location details editor | Location mode of `NativeManualFlightRouteViewController`; expanded activity page sheet, 36pt radius, visible grabber, non-expanding scrolling; existing 44pt Back and 76×40 hidden Save geometry; 20pt content insets/14pt stack spacing; extracted 76pt category card with 48pt icon, 64pt check-in/check-out cards with 60pt-minimum controls, and 52pt detail rows with 138pt labels/18pt spacing/0.5pt separators | Existing fixed UIKit typography | Phase 6E initial/existing-location presentation helpers and callback geometry are deterministic tests. Name/address and coordinates remain prefilled from the presenter-owned `MKMapItem`; no clear action or editor-local search exists. Screenshot not persisted because the production form requires an upstream live MapKit selection and saved-place segment fixture; no private MapKit completion fixture was introduced |
| Saved Place editor composition | `NativeSavedPlaceEditorView` inside the `.location` mode of `NativeManualFlightRouteViewController`; expanded activity page sheet, 36pt radius, visible grabber, non-expanding scrolling; existing 44pt Back and hidden 76×40 Save; 20pt outer insets and 14pt section spacing; 76pt category, two 48pt Name/Address rows, 64pt schedule cards, 52pt Location detail rows, and existing 64pt Cost/Note/attachment actions | Existing fixed UIKit typography | Phase 6F deterministic composition, native-field callback, schedule callback, and autosave-generation fixtures added. Initial production state remains an upstream-selected `MKMapItem` plus saved-place segment; 0.45-second generation-guarded autosave and status copy are unchanged. No screenshot persisted because the production presenter depends on live MapKit and authenticated saved-place state |
| Transportation editor composition | `NativeTransportationEditorView` for Car, Train, Car Rental, Transfer, Cruise, Walk, Bus, Bike, Ferry, and Motorcycle modes inside `NativeManualFlightRouteViewController`; expanded activity page sheet, 36pt radius, visible grabber, non-expanding scrolling; 44pt Back, visible 76×40 Save, 20pt outer insets, 14pt section spacing; 48pt primary fields, existing route cards, 52pt kind-configured reservation rows, and 64pt Cost/Note/attachment actions | Existing fixed UIKit typography | Phase 6G deterministic kind mapping, representative Train/Car Rental/Ferry configuration, field callback, and departure-before-arrival composition fixtures added. MapKit search results remain live and are not fabricated. No canonical screenshot persisted because the production form requires trip/presenter callbacks and route locations; Flight remains on its existing separate branch |
| Flight editor composition | `NativeFlightEditorView` in the Flight mode of `NativeManualFlightRouteViewController`; expanded activity page sheet, 36pt radius, visible grabber, non-expanding scrolling; 44pt Back, visible 76×40 Save, 20pt outer insets and 14pt section spacing; unbordered 18pt primary card with three 48pt Airline/IATA/Flight Number fields and 0.5pt separators; existing departure, arrival, and AR route sections; nine ordered 52pt airport/reservation detail rows; existing 64pt Cost/Note/attachment actions | Existing fixed UIKit typography | Phase 6H deterministic configuration, initial metadata/IATA behavior, field callbacks, and departure-before-arrival composition fixtures added. MapKit airport completion, IATA extraction, time-zone inference, validation, submission, and AR remain parent-owned and unchanged. No screenshot persisted because the production form requires live MapKit airport results and presenter state; no private completion fixture was fabricated |

Phase 2 migrates only the Native Itinerary timeline and more floating controls. Their measured 50×50 frame, 20pt semibold symbol, semantic colors, circular geometry, floating elevation, labels, and menus are preserved by the primitive configuration and targeted tests. A reproducible full-screen before/after capture remains pending because Native Itinerary still lacks a stable authenticated fixture harness. Image baselines must not embed production user data or depend on live Apple/network responses.

Phase 6I changed no baseline geometry or visual ownership. Saved Place, Transportation, and Flight retain the composition contracts recorded above; the parent continues to own route/domain, MapKit, date/time, validation, submission, and Flight AR orchestration. The recovered editor-family runtime baseline is 65 passed and 0 failed on iPhone 17e with iOS 26.5 on August 27, 2026.

| Phase 7 state | Geometry and conditions | Typography | Capture status |
|---|---|---|---|
| Globe sheet and controls | Custom in-view Globe sheet with collapsed/medium/expanded states; 40pt all-corner radius, 8pt horizontal inset, 12pt bottom inset, 20% upward shadow with 34pt radius and −4pt y offset. `NativeMapControlsView` remains at 20pt safe-area trailing and 18pt regular/14pt compact-height top placement; three 48pt controls, 8pt cluster spacing, 1pt grouped spacing, 30×1 separator, existing translucent neutral surfaces and 0.75pt compatibility borders | Existing fixed map-control symbols and measured Globe typography | Phase 7 deterministic geometry, callback, label, reading-order, minimum-target, and VoiceOver activation contracts added. No canonical screenshot persisted because authenticated trip state and live Apple map tiles are not stable repository pixels; map-domain behavior and tiles were not changed |
| Map Preferences | Page sheet using prominent semantic configuration with existing custom 49.4% reference detent plus large, reference selected, hidden grabber, prominent compatibility radius, large undimmed; ultra-thin material; 24pt horizontal content inset, 19pt section spacing; 34pt title, 48pt close control; two 170pt preview cards at 12pt spacing, 20pt radius and 3pt selected gold border; two existing route switches | Existing measured 34pt title and system body rows | Phase 7 deterministic sheet, measured geometry, selected-state, accessibility, and callback tests added. No image baseline persisted because `MKMapSnapshotter` preview imagery depends on Apple map data; tests validate owned overlay and card contracts instead |
| Activity annotation default/selected | 40pt circular category badge with 20pt glyph, 2pt white stroke, 20%/3pt/1.5pt-y rendering shadow; optional 132×22pt 13pt semibold label. Selected state retains −54pt center offset, title y=82, 1.72 badge scale, 1.12 glyph scale, tail and 7pt anchor dot; unselected/reused state clears all selected presentation | Existing fixed 13pt label and category glyph | Phase 7B deterministic surface, selected/unselected, focused-title, reuse-reset, and accessibility contracts added. No map screenshot persisted because tile content and dense-result placement are MapKit-dependent; activity results intentionally do not cluster |
| Trip and geographic annotations | Trip flag badge remains 40pt circular, 36pt flag, 14pt bold country label, 24%/5pt/2pt-y map-pin shadow, coordinate anchor and button semantics. Geographic labels retain 190×54pt continent/ocean typography and rendering strokes; user location remains 22pt with 3pt white border and map-pin shadow | Existing rendering-specific typography | Presentation uses shared map-pin elevation where exact; geometry, collision, display priority, coordinates, selection callbacks, and label rendering remain feature-owned. No live-map baseline persisted |
| Place Card base/travel state | Page sheet with 39.6% summary and large detents, summary selected, 38pt radius, visible grabber, scrolling expansion and large undimmed; 20pt content insets/16pt spacing; 62pt minimum header with 44pt share/close controls; 22pt cards; 56pt bottom bar. Four travel modes retain 44pt surface, 22pt outer radius, 18pt selected surface, 4pt margins and Walk/Drive/Transit/Cycle order | Existing 24pt title, 16pt subtitle/details, 22pt Travel Time title, 14pt mode durations | Phase 7B deterministic sheet, geometry, mode order, duration update, selected trait, callbacks, annotation-to-card semantics, and phone/website VoiceOver activation contracts added. No screenshot persisted because Look Around, map previews, routes, and ETA values depend on Apple services; owned presentation is covered without issuing requests |
| Globe trip card | Active height 354pt; future/past/undated height 224pt; 36pt clipping radius; full-bleed feature-owned media; gradient from clear to 78% black at locations 0.40/1.0; text inset 24pt horizontally and 42pt from bottom; title/date/status sizes 32/17/17pt using dedicated on-media roles | Existing measured on-media typography and minimum title scale 0.72 | Phase 7C deterministic geometry, gradient, metadata, decorative-media exclusion, button semantics, and shared touch/VoiceOver activation tests added. No screenshot persisted because image URL, MapKit snapshot, and destination fallback selection are runtime/domain-owned and not stable repository pixels |
| Reservation importer suggestion | Card radius uses the established card token with a 1pt system-gray compatibility border; 18pt content inset, 8pt vertical spacing, independent 36pt dismiss action, and 46pt minimum CTA | Existing 13pt eyebrow, 22pt title, 16pt body and 17pt CTA | Phase 7C extracts deterministic presentation and proves independent CTA/dismiss callbacks and accessibility order. Eligibility, visibility and routing remain controller-owned; no authenticated reservation fixture or screenshot was introduced |
| Remaining Globe circular actions | Collapsed search/add controls remain 48pt circles with legacy 8%/18pt/9pt-y elevation and ±6pt horizontal transforms; trip collection remains a native `UIMenu` trigger | Canonical icon-button symbols with explicit labels | Phase 7C removes `circularButton(...)` and consumes `AlmidyIconButton` through a feature compatibility factory. No screenshot persisted because the surrounding custom sheet and map content require runtime trip state |
| Place specialized cards | Information/contact cards retain 22pt radius, feature-specific rows and margins; route endpoints retain 36pt icon surfaces and Place-owned layout; Save retains 52pt capsule geometry and its exact feature shadow; Look Around and bottom action composition remain unchanged | Existing Place-specific title/detail/action typography | Audited in Phase 7C and intentionally retained: shared cards/list rows/buttons cannot reproduce these compositions without visual change, while media, ETA, routes, URLs and authentication remain operational dependencies |

| Phase 8 Trip Overview state | Geometry and conditions | Accessibility / capture status |
|---|---|---|
| Expanded populated | 370pt hero header, 48pt controls at 20pt sides/14pt top, 52pt country flag, 20pt outer content inset, measured action region ending at the 502.7pt first-card baseline, 28pt cards and 18pt inter-card rhythm | Existing deterministic populated fixtures and geometry contracts retained. Repository test attachments can render owned geometry, but remote hero pixels are not canonical |
| Expanded accessibility | 446pt hero header with Dynamic Type labels and horizontally scrolling shortcuts; minimum interactive target remains 44pt | Dedicated accessibility-category geometry remains component-owned and unchanged |
| Compact/collapsed | 100pt compact header or 128pt accessibility header; fully collapsed composition retains 98pt visual header, 43pt controls, 15pt outer inset, 70pt empty-action circle and 209.3pt itinerary baseline | Collapse identity handoff, fixed-toolbar geometry, custom grabber, contrast, and reading order remain specialized deterministic contracts |
| Empty actions | 72pt expanded Add First Activity circle, 34pt icon, 3pt label gap, interpolating to the measured 70pt/2pt collapsed composition | Touch and VoiceOver activation share the same callback; no generic empty-state substitution |
| Populated actions | 64pt action circles, 29pt icons, 8pt label gap and 60pt minimum control width | Component-scoped action geometry with Dynamic Type and independent button semantics |
| Overview cards | Itinerary, Imported Items, Expenses, Latest Added, Email Forwarding and Invite Guests retain 28pt radius; common horizontal inset 16pt, family-specific vertical insets and structural timeline geometry remain unchanged | Shared exact surface and generic hairline primitives adopted; specialized rows, icon clusters, category bubbles and state behavior remain feature-owned |
| Overview sheet | Fully collapsed/custom collapsed/large detents, large selected and undimmed; 34pt radius; custom in-hero grabber; scrolling does not expand; compact-height edge attachment follows content width | Uses `AlmidySheetConfiguration.overview` plus feature detents. Underlying Globe interaction/dismissal behavior is unchanged |
| Hero/media | Full-bleed focal crop at the existing 0.48 vertical anchor, image-derived terminal/compact palette, exact gradients and opacity stops, attribution filtering and generated fallback | Remote media pixels are excluded from canonical comparison; geometry, overlay and fallback behavior remain deterministic |

## Native Design System v1 frozen baseline

Frozen August 27, 2026 with **88 executed / 88 passed / 0 failed** on the iPhone 17e simulator running iOS 26.5. This is the minimum behavioral gate for every v2 phase; later tests add to rather than replace it.

The v1 baseline intentionally preserves current light-branded pixels, specialized map/media rendering, measured Trip Overview composition, accessibility-specific geometry and platform-native controls. Recommended milestone: `almidy-native-design-system-v1`. Canonical screenshots remain partial until deterministic authentication, media, MapKit, Look Around and keyboard/picker fixtures exist. See [`DESIGN_SYSTEM_V2_ROADMAP.md`](../../../DESIGN_SYSTEM_V2_ROADMAP.md) for the fixture plan and normalization risk matrix.

## v2.1 baseline note

The v2.1 exact-equivalent cleanup changes naming and removes dead compatibility APIs only. It changes no baseline geometry, canonical raw token value, appearance behavior, accessibility behavior, or approved feature override. Existing measured baselines remain authoritative without recapture. Deterministic screenshot and fixture work begins in v2.2.
